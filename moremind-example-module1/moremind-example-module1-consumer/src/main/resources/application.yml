server:
  port: 8002

## Spring Information
spring:
  application:
    name: module1-consumer
  cloud:
    nacos:
      discovery:
        enabled: true
        username: nacos
        password: nacos
        server-addr: 127.0.0.1:8848
        namespace: public
        service: ${spring.application.name}